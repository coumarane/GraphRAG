# Cursor Phase Prompt Template

Read `CURSOR.md`, all files under `specs/`, and all files under `contracts/`.

Implement **Phase N — <phase name>** from `specs/18-implementation-roadmap.md`.

Requirements:

1. Do not implement later phases except minimal stubs required for compilation.
2. List the files you will create or modify.
3. Keep domain modules independent from provider SDKs.
4. Add unit and contract tests for the phase.
5. Run the phase acceptance commands.
6. Fix every failure before finishing.
7. Finish with:
   - files changed;
   - commands run and results;
   - architectural decisions;
   - remaining risks or deferred work.
