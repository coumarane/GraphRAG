apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
  name: single-ip
  namespace: metallb-system
spec:
  addresses:
    - 51.38.19.54/32
---
apiVersion: metallb.io/v1beta1
kind: L2Advertisement
metadata:
  name: single-ip
  namespace: metallb-system
spec:
  ipAddressPools:
    - single-ip
