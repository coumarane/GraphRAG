apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app.kubernetes.io/name: chatwithdocs-api
    app.kubernetes.io/part-of: chatwithdocs
spec:
  selector:
    app.kubernetes.io/name: chatwithdocs-api
    app.kubernetes.io/part-of: chatwithdocs
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
