apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app.kubernetes.io/name: chatwithdocs-web
    app.kubernetes.io/part-of: chatwithdocs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: chatwithdocs-web
    app.kubernetes.io/part-of: chatwithdocs
  ports:
    - name: http
      port: 80
      targetPort: http
